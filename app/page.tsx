export default function Home() {
  // hono rpc/ drizzle schema into hono routes into hono client into tanstack query. fullstack typesafety.
  // drizzle zod for shadcn react hook forms.
  return (
    <main className="flex h-screen flex-col items-center justify-center ">
      <h1 className="text-4xl font-bold ">Next Auth v5</h1>
    </main>
  );
}
